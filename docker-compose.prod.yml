version: "3"
services:
  app_prod:
    build: 
      context: .
      dockerfile: Dockerfile.prod
    container_name: course-platform-app-prod
    ports:
      - 3000:3000
    volumes:
      - .:/home/node/app
      - /home/node/app/dist
      - /home/node/app/node_modules